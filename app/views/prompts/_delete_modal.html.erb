<div class="modal" id=<%= "del-#{prompt.id}" %>>
  <div class="modal-wrapper">
    <div class="modal-container">
      <div class="modal-body">
        Are you sure you want to delete this prompt?
      </div>

      <div class="modal-footer">
        <%= link_to "Cancel", "#", class: "button", data: { "close-modal": "del-#{prompt.id}" } %>
        <%= link_to "Delete", prompt_path(prompt), method: :delete, class: "button button-danger" %>
      </div>
    </div>
  </div>
</div>
