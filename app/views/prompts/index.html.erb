<div>
  <%= form_tag prompts_path, method: :post, class: "mt-10 w-100-per" do %>
    <%= text_field_tag :content, nil, placeholder: "", autocomplete: "off", required: true %>
    <%= submit_tag "Save prompt", class: "button ml-10" %>
  <% end %>
</div>

<div class="mt-50">
  <% @prompts.each do |p| %>
    <div class="mt-10">
      <div><%= p.decrypted_content %></div>

      <div>
        <%= link_to "Delete", prompt_path(p), method: :delete %>
      </div>
    </div>
    <hr>
  <% end %>

  <%= pager(@prompts, [:search]) %>
</div>
