<% if post.shared? %>
  <em class="fw-100 fs-12 text-muted">
    <% if post.listed_publicly %>
      (shared via public post)
    <% else %>
      (shared via secret link)
    <% end %>
  </em>
<% end %>
