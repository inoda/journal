<% if post.tags.any? %>
  Tags: <% post.tags.sort_by(&:decrypted_label).each_with_index do |t, i| %>
    <%= link_to t.decrypted_label, posts_path(search: t.decrypted_label) %> <%= "|" unless i == post.tags.length - 1 %>
  <% end %>
<% end %>
